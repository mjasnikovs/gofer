name: Check

on:
    push:
        branches: [master]
    workflow_dispatch:

concurrency:
    group: check-${{ github.ref }}
    cancel-in-progress: false

jobs:
    check:
        runs-on: ubuntu-24.04
        timeout-minutes: 30
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22.19.0
                  cache: npm
            - uses: dtolnay/rust-toolchain@stable
              with:
                  toolchain: 1.97.1
                  components: clippy,rustfmt
            - uses: actions/setup-python@v5
              with:
                  python-version: '3.13'
            # xvfb is not optional here: three acceptance tests run the fixture as a real windowed
            # game, because a screenshot needs a rendered frame and a headless viewport has none.
            - name: Install system dependencies
              run: >-
                  sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev
                  libappindicator3-dev librsvg2-dev patchelf unzip xvfb
            - name: Install JavaScript dependencies
              run: npm ci
            - name: Install pinned visual-test browser
              run: npx playwright install --with-deps chromium
            - name: Install pinned WebDriver browser
              id: chrome
              uses: browser-actions/setup-chrome@v2
              with:
                  chrome-version: 150.0.7871.124
                  install-chromedriver: true
            - name: Install pinned Godot
              env:
                  GOFER_GODOT_INSTALL_DIR: ${{ runner.temp }}/godot
              run: node scripts/install-godot.mjs
            # Exports GOFER_GDFORMAT, which is what makes the acceptance journey take the branch
            # where formatting actually ran. Without it that assertion accepts
            # `formatter_unavailable` and proves nothing about the formatter.
            - name: Build the pinned gdformat sidecar
              run: npm run build:gdformat
            # The whole gate runs under one virtual display rather than the Godot suite alone: the
            # runner has none, and every other suite in `check` is already headless by its own
            # configuration, so a display it does not use costs nothing.
            - name: Run required checks with zero retries
              env:
                  GOFER_CHROME_BINARY: ${{ steps.chrome.outputs.chrome-path }}
                  GOFER_CHROMEDRIVER_BINARY: ${{ steps.chrome.outputs.chromedriver-path }}
              run: xvfb-run -a npm run check

    # The same journey the nightly matrix runs on Windows and macOS, so the platforms cannot drift.
    packaged:
        uses: ./.github/workflows/packaged-journey.yml
        with:
            os: ubuntu-24.04

    rust-coverage:
        runs-on: ubuntu-24.04
        timeout-minutes: 30
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22.19.0
                  cache: npm
            - uses: dtolnay/rust-toolchain@stable
              with:
                  toolchain: nightly-2026-07-25
                  components: llvm-tools-preview
            - uses: taiki-e/install-action@v2
              with:
                  tool: cargo-llvm-cov
            - name: Install Linux Tauri dependencies
              run: >-
                  sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev
                  libappindicator3-dev librsvg2-dev patchelf
            - name: Install JavaScript dependencies
              run: npm ci
            - name: Enforce Rust line and branch coverage
              run: npm run test:coverage:rust
